<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>j10_mavlink</name>
  <version>0.1.0</version>
  <description>
    Sole owner of the flight-controller interface. Streams velocity setpoints to MAVROS and
    aggregates the scattered /mavros/* topics into one VehicleState snapshot. This is the
    only package permitted to subscribe to /mavros/*, which keeps a PX4 migration or a
    MAVROS version bump a one-package change.
  </description>
  <maintainer email="info@jnhsystems.com">JNH Systems</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>rclcpp_components</depend>
  <depend>geometry_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>std_srvs</depend>
  <depend>mavros_msgs</depend>
  <depend>j10_interfaces</depend>

  <!-- Runtime only: the bridge talks to a running mavros_node over topics and services. -->
  <exec_depend>mavros</exec_depend>
  <exec_depend>mavros_extras</exec_depend>
  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>
  <test_depend>ament_cmake_gtest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
